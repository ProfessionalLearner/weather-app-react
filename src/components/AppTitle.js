import React from "react";
import '../styles/AppTitle.css'

function AppTitle() {
    return (
        <React.StrictMode>
            <div className=''>
            <nav className="navbar">
                <h1> Welcome to Weather React App</h1>
            </nav>
            </div>
        </React.StrictMode>
    );
}

export default AppTitle;